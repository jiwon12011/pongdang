# 퐁당 — 화면 설계서 & 웹 프로토타입 명세 (보충 기획)

> `퐁당-콘셉트.md`·`퐁당-에셋명세서.md`에서 **빠져 있던 것**만 채우는 문서.
> 작성: 우리팀 (team-lead 시작 정리 + ideator 보충안 수합) / 2026-07-02
> 이 문서 기준으로 웹 프로토타입(`app/`) 구현 착수.

---

## ⓪ 이번 구현의 성격 (team-lead 결정)

- Flutter 미설치 + 에셋 검증이 목적 → **모바일 퍼스트 웹 프로토타입** (바닐라 HTML/CSS/JS, `app/` 폴더, 빌드 없음).
- 솔로 모드가 메인 경로. 멀티는 진입점 UI + 로컬 시뮬만.
- **이미지는 계속 바뀔/추가될 예정** → 원본(`assets/illustrations/individual_final/`)은 그대로 두고, `tools/build-assets.sh`가 WebP 축소본을 `app/assets/`로 생성(재실행만 하면 반영). 종 데이터·화면 매핑은 `app/js/assets-data.js` 한 곳에만 둔다.
- ⚠️ 웹에선 "잠금 중 감지"가 원천 불가 → 이 프로토의 세션 로직은 **체험 시연용**. 기획서의 "iOS 스파이크 최우선"은 별개로 유효.

## ① IA / 화면 흐름

```
[홈(수면)] ─ 꾹 2초 ─▶ [세션(잠수 중)] ─ 종료 ─▶ [영수증] ─▶ [어항]
   │                                                  ▲
   ├─ 탭바: 홈 · 도감 · 어항 · 설정                     │
   └─ "같이 퐁당" ▶ 방 코드 UI(로컬 시뮬) ──────────────┘
```

- 탭바 4개: **홈 / 도감 / 어항 / 설정**. 세션·영수증은 탭 밖의 풀스크린 모드.
- **수면 세계관(간이 채택)**: 홈 = 수면 위, 어항 = 물속. 홈↔어항 전환에만 "물에 잠기는" 전환 연출을 넣는다(다른 화면은 페이드). `prefers-reduced-motion`이면 전부 페이드.

## ② 온보딩 — "설명 없이 첫 퐁당" (ideator안 채택)

- 첫 실행: 튜토리얼 화면 없음. 빈 어항 + **"5분만 엎어볼래?"** 버튼 하나.
- 5분 온보딩 전용 미니 세션(티어 예외) 완주 → 웰컴 물고기 1마리가 퐁당 떨어지며 어항·도감·영수증이 처음 열린다.
- 닉네임 입력은 첫 물고기를 얻은 **뒤로** 미룬다.
- 세션 방식 안내 한 줄만: "화면은 켜둔 채, 그냥 뒤집어 두면 돼."

## ③ 세션 (잠수 중) — 웹 대체 규칙

- **시작 제스처**: 버튼 탭이 아니라 **화면 2초 꾹** → 누른 자리에서 파문 → 화면이 어두운 물속으로 가라앉으며 시작. 손 떼도 유지. 안내: "이제 폰을 재워줘."
- **세션 중 화면**: 초저휘도(밤 어항 톤 `#1A1F3A` 근처, 거의 검정) + 아주 흐린 물결. **경과시간 상시 표시 안 함** — 탭 1회 시 3초만 흐릿하게 표시 후 다시 어둡게.
- **"들었다" 감지 2단**:
  1. `visibilitychange` (탭 이탈·홈 이동) = 확실한 들기 → 삐짐 발동
  2. `pointerdown` = "만졌다" 보조 신호 → 삐짐 1단계만
- **삐짐 3단계** (콘셉트 ⑦ 준수, 화면 연출만): 1단 "…들었어?" → 2단 "물고기가 바위 뒤로 숨었어" → 복귀 시 "휴, 다시 왔구나. 못 본 척해줄게".
- **중단/포기**: 실패 화면 금지. 확인 1번 — "정말 그만둘까? 물고기들은 안 삐져." / 15분 미만 종료 = "오늘은 여기까지. 발끝만 담갔다 갔네." + 시도 기록 저장(삭제 안 함 원칙).
- **복귀 연출**: 종료 후 즉시 밝아지지 않고 물방울이 맺혔다 흘러내리며 1.5초에 걸쳐 밝아짐("젖은 화면").
- **세션 복원**: 진행 중 세션은 localStorage에 시작 타임스탬프 저장 — 새로고침/탭 닫기 후 재진입 시 이어짐.
- 데모 배속(설정에서 ×60): 시연 때 1분을 1초로. 프로토타입 전용.

## ④ 물고기 획득 — 지속시간 사다리 (콘셉트 ③ 그대로)

`duration_tier` 데이터: <15분 0마리(시도 기록) / 15분 1마리·흔함 / 30분 2·비범 / 60분 3·레어(비범1 보장) / 90분 4·에픽(레어1) / 120분 5·전설(에픽1) / 180분+ 5·전설1 보장+스탬프.
- 추첨은 천장(enum 비교) + 보장 등급 강제. 세션 중 화면엔 절대 안 보여주고 **영수증에서 개봉**.

## ⑤ 영수증

- 프로토타입은 **1테마 고정** (team-lead 결정). 랜덤 요소는 물고기 스탬프 배치만.
- 구성: 모노스페이스 POS 톤 + 손그림 프레임(`receipt_final_001_wobbly_receipt_frame`) — 날짜·집중시간·티어명·CATCH OF THE DAY(가장 레어한 1마리 크게)·오늘의 한 줄(자동 서정 카피)·NEXT FISH HINT.
- **세션 이름 짓기 = 영수증 개봉 직후, 스킵 가능** (ideator안 채택). 상단 제목이 점선 빈칸 — "이 시간에 이름을 붙여줄래?" 스킵 시 자동 이름("7월 2일 노을 퐁당"), 나중에 수정 가능.
- 개봉 전 카피: "젖은 손 닦고 열어봐."

## ⑥ 어항

- **시간대 4변형** — 세션 시작 순간 기준. 경계(ideator안 채택, 저녁 약속이 "노을"에 걸리도록 넓힘):
  | 변형 | 시각 | 에셋 |
  |---|---|---|
  | 새벽 | 04:00–07:00 | `aquarium_final_005_pearl_dome` + `bg_final_008_morning_mint` |
  | 낮 | 07:00–16:30 | `aquarium_individual_day_rounded` + `bg_final_001_cream_underwater_diary` |
  | 노을 | 16:30–19:30 | `aquarium_final_002_sunset_rounded` + `bg_final_003_sunset_coral_diary` |
  | 밤 | 19:30–04:00 | `aquarium_final_003_night_rounded` + `bg_final_004_night_navy_moon` |
- 레이어: `back` → 물고기(개별 배치·유영) → `front` (에셋명세서 §4 준수).
- **수용량 12마리** (모바일 성능 겸용). 넘치면 오래된 물고기부터 과거 어항으로 "이사" + 토스트 "몇 마리는 옛 어항에 놀러 갔어."
- 어항 좌우 스와이프 = 과거 세션 어항 순회 (**어항=연보**의 실체화). 물고기 핀 고정(하트)은 v1.1로.
- **빈 어항**: 결핍 금지 — 기포+손낙서 별 + "아직 아무도 안 살아. 첫 손님을 기다리는 중…"
- **물멍 모드** (ideator안 채택): 어항 화면 15초 무입력 → UI 전부 가라앉고 물고기·물결만. 탭하면 복귀.

## ⑦ 도감

- 50종 그리드. 기본 정렬 = **최근 만난 순** (도감=일기장). 필터: 희귀도 / 시간대 / 미획득.
- 미획득 = 검은 실루엣 대신 **연필 스케치 밑그림** 느낌 (프로토타입: CSS `filter: grayscale + opacity + contrast` 근사).
- 카드: 이름·희귀도·처음 만난 날·함께한 시간·(솔로면) 퐁당 일기 한 줄.
- 빈 도감: "바다는 넓고, 우린 아직 발끝만 담갔어."

## ⑧ 마이크로카피 톤 — "어항 속 물고기들이 쓰는 반말" (ideator안 채택)

규칙: ① 한 문장, 짧게 ② 명령형 금지("~해볼래?") ③ 감정 표현은 이모지 대신 손낙서 데코 PNG ④ 숫자·에러 등 기능 정보는 건조한 디지털층(3층 분리 준수).

| 상황 | 문구 |
|---|---|
| 시작 버튼 | 퐁당, 들어간다 |
| 솔로 시작 | 오늘은 나 혼자 잠수 |
| 빈 어항 | 아직 아무도 안 살아. 첫 손님을 기다리는 중… |
| 빈 도감 | 바다는 넓고, 우린 아직 발끝만 담갔어 |
| 로딩 | 물 데우는 중… / 기포 올라오는 중… |
| 삐짐 1·2단 | …들었어? / 물고기가 바위 뒤로 숨었어 |
| 복귀 | 휴, 다시 왔구나. 못 본 척해줄게 |
| 세션 완료 | 오늘의 물살, 여기 담아뒀어 |
| 15분 미만 | 오늘은 여기까지. 발끝만 담갔다 갔네 |
| 영수증 개봉 | 젖은 손 닦고 열어봐 |
| 에러/오프라인 | 물결이 잠깐 흐려졌어. 곧 맑아질 거야 |

## ⑨ 채택/보류 정리

| 항목 | 판단 |
|---|---|
| 온보딩 미니 세션·빈 상태·이름 짓기 시점·중단 UX·도감 정렬·어항 이사·시간대 경계·카피 톤·꾹 시작·2단 감지·젖은 화면·물멍 모드 | ✅ 채택 (이번 프로토) |
| 수면 인터페이스(화면 전환=잠수) | 🔶 홈↔어항 한 구간만 간이 채택 |
| 소리 우표(세션 소음 1초 각인) | 🔜 v2 백로그 (마이크 권한·프라이버시 설계 필요, 차별화 가치 높음) |
| 실제 멀티 동기화·GPS/날씨·이벤트 12종·수익화·Rive | ❌ 이번 범위 밖 (기존 로드맵대로) |

## ⑩ 기술 명세 (developer)

- **구조**: `app/index.html` + `app/css/` + `app/js/` (모듈 분리, 빌드 없음, 로컬 서버로 확인).
- **에셋**: `app/assets/` (WebP, 총 ~2.5MB). `app/assets/file-list.json` = 파일 목록 자동 생성본. **종 데이터·화면 매핑은 `app/js/assets-data.js`** — 물고기 50종의 id(파일명)·한글 이름·희귀도·태그. 이미지 교체 시: 원본 폴더에 넣고 `bash tools/build-assets.sh` → 필요하면 assets-data.js에 행 추가.
- **희귀도 배분(50종)**: 흔함 24 / 비범 13 / 레어 8 / 에픽 4 / 전설 1(`fish_final_024_golden_legendary`). 파일명 무드에 맞게 배정.
- **저장(localStorage)**: `pongdang.profile`(닉네임·온보딩 여부) / `pongdang.fish[]`(획득: speciesId·caughtAt·sessionId) / `pongdang.sessions[]`(시작·끝·티어·이름·시간대·시도기록 포함) / `pongdang.activeSession`(복원용).
- **모션**: CSS 위주(물고기 유영 루프·기포·파문·찰랑). `transform`/`opacity`만. `prefers-reduced-motion` 전체 분기. GSAP은 이번엔 안 씀(바닐라로 충분).
- **3층 분리**: 손그림 배경/데코 ↔ 글래스 카드(반투명+대비 틴트) ↔ 텍스트·숫자(명확). 팔레트·타이포는 콘셉트 ⑩ 그대로 (제목=손글씨 웹폰트, 본문=Pretendard, 숫자=IBM Plex Mono).
- **성능**: 현재 시간대 어항 레이어만 프리로드, 물고기는 보유분만 lazy, 도감 그리드는 `loading="lazy"`.
